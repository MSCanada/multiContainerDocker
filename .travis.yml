sudo: required
services:
  - docker

before_install:
  - docker build -t msuhail/react-test -f ./client/Dockerfile.dev ./client

script:
  - docker run msuhail/react-test npm test -- --coverage

after_success:
  - docker build -t msuhail/multi-client ./client
  - docker build -t msuhail/multi-nginx ./nginx
  - docker build -t msuhail/multi-server ./server
  - docker build -t msuhail/multi-worker ./worker
  # Log in to the docker CLI
  - echo "$DOCKER_PASSWORD" | docker login -u "$DOCKER_ID" --password-stdin
  # Take those images and push them to docker hub
  - docker push msuhail/multi-client
  - docker push msuhail/multi-nginx
  - docker push msuhail/multi-server
  - docker push msuhail/multi-worker

